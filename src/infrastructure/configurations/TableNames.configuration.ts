interface tableTypes {
  USERS: 'users';
  USER_SESSIONS: 'user_sessions';
}

const TableNamesConfiguration: tableTypes = {
  USERS: 'users',
  USER_SESSIONS: 'user_sessions',
};

export default TableNamesConfiguration;
